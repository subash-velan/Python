What is venv?
  venv is Python's builtin mechanism for creating isolated project environment.
  It ensures that each project has its own interpreter and dependency set, preventing global package conflicts and enabling deterministics,reproducible builds accros developers, CI pipelines and production

Usage:

> python -m venv .venv
